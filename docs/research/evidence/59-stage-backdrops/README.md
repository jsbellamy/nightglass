# Stage backdrop tile review (#59)

Scenario-keyed review artifacts for the three Stage backdrops under the
five-opponent stress case (Party + five Pipcaps + Boss, with health bars,
damage numbers, effect frames, status icons, and actor-pool chips).

**Style cohort:** no prior backdrop peers — see `STYLE-COHORT.md`
(gradient interim readability baseline + `knight` / `pipcap` / `boss-1`
combat sprite palette).

| Sheet | Stage | File |
| --- | --- | --- |
| Orchard Understory | Stage 1 | `tile-review-backdrop-1.png` (+ `@4x`) |
| Moonlit Bramble | Stage 2 | `tile-review-backdrop-2.png` (+ `@4x`) |
| Nightbloom Terrace | Stage 3 | `tile-review-backdrop-3.png` (+ `@4x`) |

Judge at native 1× first; use `@4x` only to inspect ground-band flatness and
feedback pop. Accept when bars/numbers/effects remain the brightest signals
near combatants and sprites sit on a near-flat ground band without floating.
